@*@model BudgetApp.Models.CodeFirst.HouseholdInvitation*@*@
@*@model IEnumerable<BudgetApp.Models.CodeFirst.HouseholdInvitation>

@{
    ViewBag.Title = "Create";
}

<h1>Household Dashboard</h1>

<div>
    <h2>Members</h2>

    <table class="table table-striped" id="ticketIndexTable">
        <thead>
            <tr>
                <th>
                    Name
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Email)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.DisplayName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Email )
                    </td>
                    
                </tr>

            }
        </tbody>
        </table>
</div>

<div>

    <h2>Create Invitation</h2>

    @using (Html.BeginForm("Invite", "Households"))
    {
        @Html.AntiForgeryToken()

        <div class="form-horizontal">
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            <div class="form-group">
                <div class="col-md-10">
                    <h3>Enter Email Below</h3>
                    @Html.TextAreaFor(model => model.Email, new { htmlAttributes = new { @class = "form-control", required = "required", placeholder = "Email" } })
                    @Html.ValidationMessageFor(model => model.Email, "", new { @class = "text-danger" })
                </div>
            </div>
            <div class="form-group">
                <div class="col-md-offset-2 col-md-10">
                    <input type="submit" value="Invite" class="btn btn-primary" />
                </div>
            </div>
        </div>
    }
</div>
<div>
    <h2>Leave Household</h2>
    <p>Leave your current household and start a new one.</p>
    <input type="submit" value="Leave" class="btn btn-primary" />
</div>

<div>
    @Html.ActionLink("Back to List", "Index")
</div>*@*@*@

